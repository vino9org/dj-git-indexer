{
  "packages": [
    "python@3.10",
    "poetry@1.4.2"
  ],
  "shell": {
    "init_hook": [
      "poetry install",
      "poetry shell"
    ],
    "scripts": {
      "idx": "poetry run python manage.py index --source gitlab --query 'securitybankph/' --filter '*' --export-csv db/all_commit_data.csv",
      "app": "poetry run python manage.py runserver",
      "test": "poetry run pytest -v --cov=. --cov-report=html"
    }
  }
}
